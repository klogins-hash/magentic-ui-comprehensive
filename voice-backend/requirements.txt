# Pipecat framework with all necessary extensions
pipecat-ai[groq,daily,websocket,silero,runner]>=0.0.50

# Core dependencies
python-dotenv>=1.0.0
loguru>=0.7.0
httpx>=0.25.0

# FastAPI for REST endpoints (optional, for compatibility)
fastapi>=0.104.0
uvicorn>=0.24.0

# Audio processing
numpy>=1.24.0
torch>=2.0.0

# WebRTC and transport support
aiortc>=1.6.0
aiohttp>=3.9.0

# Performance optimizations
uvloop>=0.19.0
orjson>=3.9.0

# Additional Pipecat services
groq>=0.4.0
openai>=1.0.0  # Fallback TTS if needed
